// This script sets the configuration of the frontend,
// for example it specifies the address of the server.

window._config = {
	// backend: "https://draughts-api.example.com"
	backend: `${window.location.origin}/api`
};
